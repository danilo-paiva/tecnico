// URL base da API (mesma origem quando rodada com `composer start`).
// Se o front for aberto de outro servidor/porta, ajuste aqui.
const API_BASE_URL = window.API_BASE_URL || `${window.location.protocol}//${window.location.hostname}:8080`;
